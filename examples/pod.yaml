apiVersion: v1
kind: Pod
metadata:
  name: protected-api
  namespace: production
  annotations:
    teelens.io/attestation-policy: production-ml-v3
spec:
  runtimeClassName: kata-qemu-coco
  containers:
  - name: app
    image: example.invalid/protected-api:latest
    resources:
      requests:
        nvidia.com/gpu: "1"
