# Security policy

Do not report vulnerabilities in public issues. Report a reproducible description,
affected TeeLens version, and impact to the repository owner through GitHub's private
security reporting feature when it is enabled.

TeeLens must never emit attestation evidence, private keys, workload environment
variables, guest memory, or unredacted hypervisor command lines.
